# Public Assets
This folder contains the static assets for the React application, such as the `index.html` file where the React app mounts, and any public images or resources that don't need to go through the webpack/Vite build process.
